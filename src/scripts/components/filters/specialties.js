'use strict';

var React = require('react');

var SpecialtyFilter = React.createClass({

  render: function() {
    return (
      <div>
      </div>
    );
  }

});

module.exports = SpecialtyFilter;
