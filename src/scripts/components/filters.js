'use strict';

var React = require('react');

var Filters = React.createClass({

  render: function() {
    return (
      <div className="filters">
      </div>
    );
  }

});

module.exports = Filters;
